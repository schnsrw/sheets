diff --git a/lib/cjs/index.js b/lib/cjs/index.js
index 3f4557fa19b49ec00d5f511ace2d301997ec9012..01455ea2574c819195f1c7935d0967e2a84a6ee4 100644
--- a/lib/cjs/index.js
+++ b/lib/cjs/index.js
@@ -3202,11 +3202,13 @@ var HtmlToUSMService = class HtmlToUSMService {
 				v: value.richTextParma.v,
 				p: value.richTextParma.p,
 				s: style,
+				f: value.formula,
 				rowSpan: value.rowSpan,
 				colSpan: value.colSpan
 			} : {
 				v: value.content,
 				s: style,
+				f: value.formula,
 				rowSpan: value.rowSpan,
 				colSpan: value.colSpan
 			};
@@ -3261,6 +3263,8 @@ var HtmlToUSMService = class HtmlToUSMService {
 				let cellText = "";
 				let cellRichStyle;
 				let numfmtPattern;
+				const formulaMatch = cell.outerHTML.match(/\b(?:x:fmla|fmla)\s*=\s*(?:"([^"]*)"|'([^']*)'|(\S+))/i);
+				const formula = (formulaMatch === null || formulaMatch === void 0 ? void 0 : formulaMatch[1]) || (formulaMatch === null || formulaMatch === void 0 ? void 0 : formulaMatch[2]) || (formulaMatch === null || formulaMatch === void 0 ? void 0 : formulaMatch[3]) || void 0;
 				const pattern = this._getMsoNumfmtForNode(cell);
 				if (pattern) {
 					cellText = cell.innerHTML;
@@ -3289,6 +3293,7 @@ var HtmlToUSMService = class HtmlToUSMService {
 					rowSpan,
 					colSpan,
 					content: cellText,
+					formula,
 					style: cellStyle,
 					richTextParma: {
 						p: cellRichStyle,
@@ -4452,11 +4457,13 @@ let SheetClipboardService = class SheetClipboardService extends _univerjs_core.D
 		const filteredHooks = enabledHooks.filter((h) => !h.specialPasteInfo && h.id !== PREDEFINED_HOOK_NAME_PASTE.DEFAULT_PASTE || pasteType === h.id);
 		filteredHooks.forEach((h) => {
 			var _h$onPasteRows, _h$onPasteColumns, _h$onPasteCells;
+			// PATCH(casual-sheets): see lib/es/index.js — pass rowProperties +
+			// payload so clipboard hooks can read row heights on a regular paste.
 			const rowReturn = (_h$onPasteRows = h.onPasteRows) === null || _h$onPasteRows === void 0 ? void 0 : _h$onPasteRows.call(h, {
 				range: pastedRange,
 				unitId,
 				subUnitId
-			});
+			}, rowProperties || [], { pasteType });
 			if (rowReturn) {
 				redoMutationsInfo.push(...rowReturn.redos);
 				undoMutationsInfo.push(...rowReturn.undos);
diff --git a/lib/es/index.js b/lib/es/index.js
index 02b57bed9a3503688aea74393dffb652f4b0eabc..2bb819505e6e984b04644db7cc913a36caa228bb 100644
--- a/lib/es/index.js
+++ b/lib/es/index.js
@@ -3173,11 +3173,13 @@ var HtmlToUSMService = class HtmlToUSMService {
 				v: value.richTextParma.v,
 				p: value.richTextParma.p,
 				s: style,
+				f: value.formula,
 				rowSpan: value.rowSpan,
 				colSpan: value.colSpan
 			} : {
 				v: value.content,
 				s: style,
+				f: value.formula,
 				rowSpan: value.rowSpan,
 				colSpan: value.colSpan
 			};
@@ -3232,6 +3234,8 @@ var HtmlToUSMService = class HtmlToUSMService {
 				let cellText = "";
 				let cellRichStyle;
 				let numfmtPattern;
+				const formulaMatch = cell.outerHTML.match(/\b(?:x:fmla|fmla)\s*=\s*(?:"([^"]*)"|'([^']*)'|(\S+))/i);
+				const formula = (formulaMatch === null || formulaMatch === void 0 ? void 0 : formulaMatch[1]) || (formulaMatch === null || formulaMatch === void 0 ? void 0 : formulaMatch[2]) || (formulaMatch === null || formulaMatch === void 0 ? void 0 : formulaMatch[3]) || void 0;
 				const pattern = this._getMsoNumfmtForNode(cell);
 				if (pattern) {
 					cellText = cell.innerHTML;
@@ -3260,6 +3264,7 @@ var HtmlToUSMService = class HtmlToUSMService {
 					rowSpan,
 					colSpan,
 					content: cellText,
+					formula,
 					style: cellStyle,
 					richTextParma: {
 						p: cellRichStyle,
@@ -4423,11 +4428,15 @@ let SheetClipboardService = class SheetClipboardService extends Disposable {
 		const filteredHooks = enabledHooks.filter((h) => !h.specialPasteInfo && h.id !== PREDEFINED_HOOK_NAME_PASTE.DEFAULT_PASTE || pasteType === h.id);
 		filteredHooks.forEach((h) => {
 			var _h$onPasteRows, _h$onPasteColumns, _h$onPasteCells;
+			// PATCH(casual-sheets): pass rowProperties (Univer's parser fills
+			// them from `<tr height="…">` / inline styles) and the payload so
+			// clipboard hooks can read row heights on a regular paste, the
+			// same way onPasteColumns already gets colProperties.
 			const rowReturn = (_h$onPasteRows = h.onPasteRows) === null || _h$onPasteRows === void 0 ? void 0 : _h$onPasteRows.call(h, {
 				range: pastedRange,
 				unitId,
 				subUnitId
-			});
+			}, rowProperties || [], { pasteType });
 			if (rowReturn) {
 				redoMutationsInfo.push(...rowReturn.redos);
 				undoMutationsInfo.push(...rowReturn.undos);
