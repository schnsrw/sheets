diff --git a/lib/cjs/index.js b/lib/cjs/index.js
index b77b6472231c5dc733fa6578678ec20b6e84e999..ec7eb23570ae3fa8b3fd540c6db894b849a1d477 100644
--- a/lib/cjs/index.js
+++ b/lib/cjs/index.js
@@ -208,7 +208,8 @@ let SheetsTableUiService = class SheetsTableUiService extends _univerjs_core.Dis
 		const map = /* @__PURE__ */ new Map();
 		let allItemsCount = 0;
 		for (let row = startRow; row <= endRow; row++) {
-			if (worksheet.isRowFiltered(row)) continue;
+			// PATCH(casual-sheets): see lib/es/index.js — walk every row so the
+			// dropdown can re-show values this column already filtered out.
 			let stringItem = this._sheetTableService.getCellValueWithConditionType(worksheet, row, column);
 			if (stringItem === void 0) stringItem = this._localeService.t("sheets-table.condition.empty");
 			if (!map.has(stringItem)) data.push({
diff --git a/lib/es/index.js b/lib/es/index.js
index bf5b7ac0e58da41b72f8e845509f57ad6078dc6a..3a2d025344afa8dc96c1a6ef06c0e1d1b742a21d 100644
--- a/lib/es/index.js
+++ b/lib/es/index.js
@@ -207,7 +207,11 @@ let SheetsTableUiService = class SheetsTableUiService extends Disposable {
 		const map = /* @__PURE__ */ new Map();
 		let allItemsCount = 0;
 		for (let row = startRow; row <= endRow; row++) {
-			if (worksheet.isRowFiltered(row)) continue;
+			// PATCH(casual-sheets): originally skipped rows where
+			// worksheet.isRowFiltered(row) is true, which includes rows hidden
+			// by THIS column's own filter — so reopening the dropdown lost the
+			// values that were filtered out. Walk every row so users can re-check
+			// previously-unchecked values, matching Excel's behavior.
 			let stringItem = this._sheetTableService.getCellValueWithConditionType(worksheet, row, column);
 			if (stringItem === void 0) stringItem = this._localeService.t("sheets-table.condition.empty");
 			if (!map.has(stringItem)) data.push({
